{
  "name": "sara-via-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web & npm run start:dev -w apps/server",
    "build": "npm run build -w apps/web && npm run build -w apps/server",
    "test": "npm run test -w apps/server && npm run test -w apps/web --if-present",
    "lint": "npm run lint -w apps/web && npm run lint -w apps/server",
    "format": "npm run format -w apps/web && npm run format -w apps/server",
    "prisma:generate": "npx prisma generate --schema=./prisma/schema.prisma",
    "prisma:migrate": "npx prisma migrate dev --schema=./prisma/schema.prisma"
  }
}
